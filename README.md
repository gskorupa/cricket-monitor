# Website Monitoring Service

This is an example microservice build on Cricket Microservice Framework.

## Quick start

1. Build: `ant dist`
2. Run: `java -jar dist/basicservice-1.0.0.jar -r`
3. Shutdown with `Ctrl-C`